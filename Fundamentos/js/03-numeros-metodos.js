// Números en JavaScript

const numero1 = 30,
    numero2 = 20,
    numero3 = 20.20,
    numero4 = .1020,
    numero5 = -3;

let resultado;

// Suma
resultado = numero1 + numero2;

// Resta
resultado = numero1 - numero2;

// Multiplicar
resultado = numero1 * numero2;

// Divisiones
resultado = numero1 / numero2;

// Módulo
resultado = numero1 % numero2;

// PI
resultado = Math.PI;

// Redondeo
resultado = Math.round(2.5); // también está ceil y floor

// Raíz cuadrada
resultado = Math.sqrt(144);

// Valor Absoluto
resultado = Math.abs(numero5);

// Potencia
resultado = Math.pow(8, 3);

// Mínimo
resultado = Math.min(3, 6, 5, 1, 9, 7, 8);

// Máximo
resultado = Math.max(3, 6, 5, 1, 9, 7, 8);

// Aleatorio
resultado = Math.random();

resultado = (10 + 20) * 5;

resultado = (10 + 20 + 20 + 10 + 40) * .20;

let puntaje;

console.log(resultado);